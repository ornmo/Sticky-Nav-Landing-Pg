This project is a sticky nav on a landing page. As the User scrolls down the page the nav gets removes padding and changes colour.
the project also includes an Hero image with text.

It uses Flexbox, CSS transitions and JavaScript

This could be converted to be used in (for example) the following ways

- sticky nav
- landing page for website